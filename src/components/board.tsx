// import { BoardType } from "../types/types";
// import Cell from "./cell";

// type Props = {
//   board: BoardType;
//   onCellChange: (row: number, col: number, value: number) => void;
// };

// function Board({ board, onCellChange }: Props) {
//   return (

//   );
// }

// export default Board;
